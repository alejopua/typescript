"use strict";
(() => {
    var _a;
    let batman = 'Batman';
    let spiderman = "Spiderman";
    let blackwidow = `Black Widow`;
    console.log(`They are ${batman.toUpperCase()}, ${spiderman} and ${blackwidow}`);
    console.log(((_a = batman[30]) === null || _a === void 0 ? void 0 : _a.toLowerCase()) || 'No existe'); // null check operator ?.
})();
